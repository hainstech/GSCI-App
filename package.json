{
  "name": "gsci-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ci:test": "lerna run ci:test --stream",
    "dev": "lerna run dev --parallel",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "ci:deploy": "lerna run ci:deploy"
  },
  "version": "0.1.0",
  "author": "Hainstech",
  "license": "GPLv3",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
